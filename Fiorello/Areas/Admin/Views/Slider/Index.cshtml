@model List<Slider>
@{
    ViewData["Title"] = "Index";
}

<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Categories</h4>
            <a asp-controller="Slider" asp-action="Create" class="btn btn-success">
                Create
            </a>
            <div class="table-responsive">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>
                                Category Name
                            </th>
                            <th>
                                Settings
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (Slider slide in Model)
                        {
                            <tr>
                                <td>
                                    <img src="~/img/@slide.Image" width="150" />
                                </td>
                                <td>
                                    <a asp-controller="Slider" asp-action="Detail" asp-route-id="@slide.Id" class="btn btn-success">
                                        Detail
                                    </a>
                                    <a asp-controller="Slider" asp-action="Update" asp-route-id="@slide.Id" class="btn btn-warning">Update</a>
                                    <a asp-controller="Slider" asp-action="Delete" asp-route-id="@slide.Id" class="btn btn-danger">Delete</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
